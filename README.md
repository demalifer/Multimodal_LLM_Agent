# Multimodal_LLM_Agent

This is a multimodal llm agent. [You can ask it anything!

## Streamlit Demo

Run:

```bash
streamlit run streamlit_app.py
```

Default backend API URL is `http://127.0.0.1:8000/chat` and the app sends:

- form field: `question`
- file field: `image`
